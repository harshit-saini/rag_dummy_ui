# Copy this file to .env if you want to connect the UI to a real backend.
# If VITE_API_BASE_URL is left empty (or this file is not copied at all),
# the app automatically falls back to dummy/mock data - no backend needed.

VITE_API_BASE_URL=
